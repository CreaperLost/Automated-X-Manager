"""Settings tab was removed in the v2 simplification.

Its contents (model picker + Projects CSV editor) moved to a
collapsed "Settings" expander in the sidebar (`ui/layout.py`).
This file is kept only as a placeholder so the package import in
tests still resolves. It is not wired into the app.
"""
